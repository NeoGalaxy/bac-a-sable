# Challenge Open Data - Client Svelte

Installer les dépendances : `npm i`.

Pour générer le dossier statique : `npm run build`, les fichiers sont ensuite dans le dossier `build`.

Travailler sur le projet : `npm run dev`.
